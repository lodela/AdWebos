/* function doGet() {
  var template = components.getTemplate('index_HAA');
  return template.evaluate()
                 .setTitle('Adaevos Web App')
                 .addMetaTag('viewport', 'width=device-width, initial-scale=1');
} */

function test(){
  Logger.log(requests.getRequests());

}